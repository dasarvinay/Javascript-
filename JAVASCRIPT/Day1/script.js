// .console.log(a);

// Comment:ctrc 
// There are two types of comments in JavaScript.
// Single-line Comment
// Multi-line Comment

// three ways of rendering(retruning)data on document

// 1.console.log
// 2.alert
// 3.prompt

// alert("hi, welcome my world")

// let a= prompt("enter your name")
// alert("hi," +a)


// variable in javascript
// variable in javascript- storing information we need 
//  a variable is a container for a value, like, a number we might use in sum, or a string thast we might use as oer senatence.

// container and box where we store in different type of value or data

// JavaScript Variables can be declared in 4 ways:

//1-Automatically
//2-Using var
//3-Using let
//4-Using const

// 1995 to 2015- var keyworld to define a variable


// All JavaScript variables must be identified with unique names.

// These unique names are called identifiers.

// Identifiers can be short names (like x and y) or more descriptive names (age, sum, totalVolume).

// The general rules for constructing names for variables (unique identifiers) are:

// Names can contain letters, digits, underscores, and dollar signs.
// Names must begin with a letter.
// Names can also begin with $ and _ (but we will not use it in this tutorial).
// Names are case sensitive (y and Y are different variables).
// Reserved words (like JavaScript keywords) cannot be used as names.
// we should not used innbuliud keywords like break, const, new, continue, let